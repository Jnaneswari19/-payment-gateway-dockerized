version: '3'
services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"

  webhook:
    build: ./webhook
    ports:
      - "8081:8081"

  sdk:
    build: ./sdk
    ports:
      - "8082:8082"
